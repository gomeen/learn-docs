# 7.7.4 IM 即时通讯：WebSocket

> 理解 ruoyi IM 即时通讯模块的设计。

## 🎯 学习目标

完成本文档后，你将能够：
- 掌握 ruoyi IM 模块的设计
- 理解一对一聊天、群聊、消息存储
- 学会 WebSocket 长连接
- 能实现自定义 IM 功能

## 📚 前置知识

- WebSocket 协议（详见 [WebSocket Starter](../03-spring-boot-starters/46-websocket.md)）
- 站内信（详见 [站内信](./22-in-site-message.md)）
- Redis Pub/Sub（详见 [Pub/Sub 与 Stream](../../_common/01-redis/06-pubsub-stream.md)）
- JWT Token 鉴权（详见 [JWT](../../_common/07-authentication/03-jwt.md)）

## 1. 核心概念

### 1.1 IM 核心概念

| 概念 | 说明 |
|------|------|
| 会话 | 一对一聊天或群聊 |
| 消息 | 文本、图片、文件 |
| 未读数 | 每个会话的未读消息数 |
| 在线状态 | 用户是否在线 |

### 1.2 ruoyi IM 架构

```
[客户端 A] --WebSocket--> [IM Server] --WebSocket--> [客户端 B]
                            ↓
                       [Redis Pub/Sub]
                            ↓
                       [消息存储 MySQL]
```

**特点**：
- WebSocket 长连接
- 消息持久化到 MySQL
- Redis Pub/Sub 支持多节点

### 1.3 核心表

```sql
-- 消息
CREATE TABLE im_message (
    id BIGINT,
    conversation_id BIGINT,
    from_user_id BIGINT,
    to_user_id BIGINT,
    type INT,         -- 类型：文本/图片/文件
    content TEXT,
    read_status BOOLEAN,
    create_time DATETIME
);
```

## 2. 代码示例

### 2.1 WebSocket 握手

```java
@Component
@ServerEndpoint("/ws/im/{token}")
public class ImWebSocketHandler {

    @OnOpen
    public void onOpen(Session session, @PathParam("token") String token) {
        // 1. 校验 token
        Long userId = JwtUtil.parseToken(token);
        // 2. 保存连接
        SESSION_POOL.put(userId, session);
        // 3. 通知上线
        sendToFriends(userId, "我上线了");
    }

    @OnMessage
    public void onMessage(String message, Session session) {
        // 1. 解析消息
        ImMessageBO msg = JSON.parseObject(message, ImMessageBO.class);
        // 2. 持久化
        messageService.saveMessage(msg);
        // 3. 推送
        sendToUser(msg.getToUserId(), message);
    }

    @OnClose
    public void onClose(Session session) {
        // 清理连接
    }
}
```

### 2.2 消息发送

```java
public void sendMessage(ImMessageSendReqVO reqVO) {
    // 1. 保存消息
    ImMessageDO message = new ImMessageDO();
    message.setFromUserId(getLoginUserId());
    message.setToUserId(reqVO.getToUserId());
    message.setType(reqVO.getType());
    message.setContent(reqVO.getContent());
    messageMapper.insert(message);
    // 2. 推送
    if (isOnline(reqVO.getToUserId())) {
        sendToUser(reqVO.getToUserId(), message);
    }
    // 3. 增加未读数
    redisTemplate.opsForValue().increment(
        "im:unread:" + reqVO.getToUserId() + ":" + getLoginUserId());
}
```

### 2.3 消息已读

```java
@PostMapping("/read")
public CommonResult<Boolean> markRead(@RequestParam Long conversationId) {
    messageService.markRead(getLoginUserId(), conversationId);
    return success(true);
}
```

## 3. 关键要点总结

- ruoyi IM 通过 WebSocket 实现
- 消息持久化到 MySQL
- 在线状态通过 Redis 管理
- 支持一对一聊天、群聊
- 未读数独立计算

---

**文档版本**：v1.0
**最后更新**：2026-07-13

# 7.7.8 AI 大模型（Spring AI）

> 理解 ruoyi 的 AI 大模型集成模块。

## 🎯 学习目标

完成本文档后，你将能够：
- 掌握 ruoyi AI 模块的设计
- 理解 Spring AI 框架的核心概念
- 学会接入多种大模型（ChatGLM、文心一言等）
- 能实现自定义 AI 应用

## 📚 前置知识

- LLM 基础
- Spring AI 框架
- 26-spu-sku.md（参考业务场景）

## 1. 核心概念

### 1.1 Spring AI 是什么？

**Spring AI** 是 Spring 官方推出的 AI 应用开发框架，提供了统一的 API：
- 聊天补全（Chat Completion）
- Embedding
- 图像生成
- 音频转文字
- 工具调用（Function Calling）

### 1.2 ruoyi AI 模块结构

**文件位置**：`/Users/xu/code/github/ruoyi-vue-pro/yudao-module-ai/`

```
yudao-module-ai/
├── controller/admin/
│   ├── chat/             # 聊天
│   ├── image/            # 图像
│   ├── knowledge/        # 知识库
│   └── tool/             # 工具
├── dal/
└── service/
```

### 1.3 核心概念

| 概念 | 说明 |
|------|------|
| Chat Model | 聊天模型抽象 |
| Prompt | 输入提示 |
| Message | 消息（用户/助手/系统） |
| Token | 模型处理单位 |
| Embedding | 文本向量化 |
| RAG | 检索增强生成 |

## 2. 代码示例

### 2.1 聊天接口

```java
@Tag(name = "管理后台 - AI 聊天")
@RestController
@RequestMapping("/ai/chat")
@Validated
public class AiChatController {

    @Resource
    private ChatModel chatModel;

    @PostMapping("/send")
    public CommonResult<String> sendMessage(@Valid @RequestBody AiChatSendReqVO reqVO) {
        // 1. 构造消息
        List<Message> messages = List.of(
            new SystemMessage("你是一个助手"),
            new UserMessage(reqVO.getMessage())
        );
        // 2. 调用模型
        ChatResponse response = chatModel.call(new Prompt(messages));
        // 3. 返回结果
        return success(response.getResult().getOutput().getContent());
    }
}
```

### 2.2 流式响应（SSE）

```java
@GetMapping(value = "/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public SseEmitter streamChat(@RequestParam String message) {
    SseEmitter emitter = new SseEmitter();
    // 流式调用
    Flux<ChatResponse> flux = chatModel.stream(new Prompt(message));
    flux.subscribe(
        chunk -> {
            try {
                emitter.send(chunk.getResult().getOutput().getContent());
            } catch (IOException e) {
                emitter.completeWithError(e);
            }
        },
        emitter::completeWithError,
        emitter::complete
    );
    return emitter;
}
```

### 2.3 RAG 知识库

```java
public class AiKnowledgeService {

    @Resource
    private EmbeddingModel embeddingModel;

    @Resource
    private VectorStore vectorStore;

    public void addDocument(String content) {
        // 1. 切割文档
        List<Document> chunks = splitter.split(content);
        // 2. Embedding
        // 3. 存入向量数据库
        vectorStore.add(chunks);
    }

    public String query(String question) {
        // 1. Embedding 问题
        // 2. 向量搜索
        List<Document> docs = vectorStore.similaritySearch(question);
        // 3. 构造 prompt
        String prompt = buildRagPrompt(question, docs);
        // 4. 调用 LLM
        return chatModel.call(prompt);
    }
}
```

### 2.4 多模型接入

```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      base-url: https://api.openai.com
    dashscope:  # 阿里云百炼
      api-key: ${DASHSCOPE_API_KEY}
    zhipu:      # 智谱 AI
      api-key: ${ZHIPU_API_KEY}
```

## 3. ruoyi 仓库源码解读

### 3.1 AI 模块结构

**文件位置**：`/Users/xu/code/github/ruoyi-vue-pro/yudao-module-ai/`

```
yudao-module-ai/
├── src/main/java/.../ai/
│   ├── controller/admin/
│   │   ├── chat/        # 聊天
│   │   ├── image/       # 图像
│   │   ├── knowledge/   # 知识库
│   │   └── tool/        # 工具
│   ├── dal/dataobject/  # 对话、消息、知识库
│   ├── framework/       # Spring AI 封装
│   └── service/
```

### 3.2 聊天服务

```java
@Service
public class ChatServiceImpl implements ChatService {

    @Override
    public Flux<String> chatStream(Long userId, String conversationId, String message) {
        // 1. 加载历史消息
        List<Message> history = messageService.getHistory(conversationId);
        // 2. 构造 prompt
        Prompt prompt = new Prompt(history);
        // 3. 流式调用
        return chatModel.stream(prompt)
            .map(chunk -> chunk.getResult().getOutput().getContent())
            .doOnNext(text -> messageService.append(conversationId, text));
    }
}
```

### 3.3 知识库管理

```java
@PostMapping("/create")
public CommonResult<Long> createKnowledge(@Valid @RequestBody AiKnowledgeSaveReqVO createReqVO) {
    return success(knowledgeService.createKnowledge(createReqVO));
}

public void addDocument(Long knowledgeId, String content) {
    // 1. 文档切割
    List<Document> docs = splitter.split(Document.builder().content(content).build());
    // 2. 存入向量库
    vectorStore.add(docs);
}
```

## 4. 关键要点总结

- ruoyi AI 集成 Spring AI
- 支持多种大模型（OpenAI、智谱、阿里云等）
- 流式响应通过 SSE
- RAG 通过向量数据库实现
- 对话历史持久化

## 5. 练习题

### 练习 1：基础（必做）

解释 Chat Completion 和 Embedding 的区别。

### 练习 2：进阶

阅读 `ChatServiceImpl.java`，理解流式聊天实现。

### 练习 3：挑战（选做）

设计"AI 商品客服"：用户问商品问题，从商品知识库中检索答案。说明架构和实现步骤。

## 6. 参考资料

- `/Users/xu/code/github/ruoyi-vue-pro/yudao-module-ai/`
- Spring AI 文档：https://docs.spring.io/spring-ai/reference/

---

**文档版本**：v1.0
**最后更新**：2026-07-13
